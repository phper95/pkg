version: '3'
services:
  mongodb:
    image: mongo:4.4.2
    hostname: 0.0.0.0
    environment:
      - MONGO_INITDB_ROOT_USERNAME=admin
      - MONGO_INITDB_ROOT_PASSWORD=admin123
    restart: on-failure
    ports:
      - 27017:27017
    volumes:
      - ./mongodb:/data/db